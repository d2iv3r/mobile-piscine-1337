import 'package:flutter/material.dart';

class AppColors {
  // Dark sky background gradient
  static const bgTop    = Color(0xFF0B1120);
  static const bgBottom = Color(0xFF1C3A5E);

  // Accent
  static const accent   = Color(0xFFFFAA33);
  static const accentCool = Color(0xFF4FC3F7);

  // Text
  static const textPrimary   = Colors.white;
  static const textSecondary = Color(0xFFB0BEC5);

  // Card / surface
  static const surface = Color(0x33FFFFFF); // white 20% opacity

  // Chart lines
  static const chartMax = Color(0xFFFF6B6B);
  static const chartMin = Color(0xFF4FC3F7);
}