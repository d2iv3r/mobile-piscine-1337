name: advanced_weather_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  http: ^1.2.0
  geolocator: ^11.0.0
  fl_chart: ^0.69.0

flutter:
  uses-material-design: true
